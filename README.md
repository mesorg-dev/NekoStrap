# NekoStrap — кастомный лаунчер Roblox

[![Release](https://img.shields.io/github/v/release/mesorg-dev/NekoStrap?style=flat-square)](https://github.com/mesorg-dev/NekoStrap/releases)
![.NET 8](https://img.shields.io/badge/.NET-8-512BD4?style=flat-square)
![WPF](https://img.shields.io/badge/UI-WPF-000000?style=flat-square)
![RU/EN](https://img.shields.io/badge/lang-RU%20%7C%20EN-blue?style=flat-square)
![License](https://img.shields.io/badge/license-MIT-green?style=flat-square)

Один exe на .NET 8 и WPF: ставит и обновляет клиент Roblox, держит моды и
FastFlags в профилях, помнит, где ты играл, и показывает это в Discord.
Без установки, без реестра, без аккаунта — запустил и работает.

Telegram: https://t.me/mesorgdev

## Скачать

[NekoStrap.exe из последнего релиза](https://github.com/mesorg-dev/NekoStrap/releases)
— Windows 10/11 x64, ничего ставить не надо. Лаунчер обновляется сам:
увидел новую версию → предложил поставить из раздела «О программе».

## Что нового в 1.3.0

- **Параллельная загрузка клиента.** Пакеты качаются нитками (1–8 потоков
  в настройках), с ретраями и возобновлением с места обрыва — оборвался
  на середине, запустил снова, и пойдёт с того же байта.
- **Авто-профили на игру.** Привязал набор модов и профиль флагов к PlaceId —
  при запуске этого плейса они подставляются сами, отвязка в один клик.
- **Чистый запуск.** Кнопка «Чисто» стартует без модов и без флагов, чтобы
  найти, в чём краш. Если Roblox упал с ненулевым кодом, лаунчер предлагает
  перезапуск чисто и пишет код ошибки.
- **Экспорт и импорт профиля модов в zip.** Корень архива — зеркало клиента,
  другу отдал, он импортировал и получил ровно тот же набор.
- **Фоновое обновление Roblox.** Тумблер в настройках (по умолчанию выкл.):
  пока лаунчер открыт, свежая версия ставится в фоне, к запуску всё готово.
- **Проверка целостности и ремонт.** При установке пишется снимок файлов
  версии, дальше сверка: пропавшие, пустые, обрезанные — с учётом модов.
  «Ремонт» переставляет клиент с нуля и заново накладывает моды.
- **Discord Rich Presence.** Название игры, сервер, иконка плейса, время
  сессии, кнопка «Открыть игру» и вход к другу на твой сервер. Всё это
  включается по отдельности; ID приложения зашит в код, настройки прятать
  незачем. По умолчанию выключен.
- **Поиск игр.** Название → живой список из Roblox на Главной: автор,
  сколько сейчас играют, иконка. Двойной клик — открыл плейс.
- **Настройки разбиты на разделы:** основное, Discord, игра, трей,
  кастомизация, дополнения, система.

## Возможности

**Главная** — установка и проверка клиента, «Играть» с прогрессом, статус
и статы, поиск игр, последняя игра с «Играть снова», карточка сервера
(IP, гео, пинг — как в Shift+F5), избранные плейсы, чистый запуск.

**Моды** — профили (инстансы набора файлов, пак текстур на один клик),
быстрая замена звука смерти/шрифтов/курсора/текстур, вкл/выкл/удаление,
экспорт и импорт профиля в zip, авто-профиль под игру, Fleasion
с автозапуском. Смена профиля сначала откатывает прошлые правки
(manifest + бэкапы) и только потом накладывает новый набор.

**FastFlags** — таблица флагов активной версии, живой поиск, 16 пресетов
(FPS 60/144/240, свет, MSAA, Vulkan…), именованные профили, импорт/экспорт
JSON, перетаскивание строк, авто-профиль под игру.

**Версии** — установленные клиенты: активация, откат на предыдущую,
удаление, Roblox Studio. Параллельная загрузка с возобновлением, фоновое
обновление, проверка целостности и ремонт.

**История** — наиграно по каждому плейсу, лог заходов (PlaceId, JobId,
время, длительность, IP), избранное. «На тот же сервер» и сервер-хоп
на случайный сервер того же плейса.

**Discord** — статус с названием игры, сервером, иконкой и временем сессии;
кнопка «Открыть игру» на страницу плейса или вход к другу на твой сервер.
Название, сервер, иконка, время и кнопки настраиваются по отдельности.

**Трей, музыка, цветокор** — сворачивание в трей, меню с перезаходом
и сервер-хопом; плеер папки `Music` (шаффл, громкость); цветокоррекция
экрана через Magnification API с пресетами и режимом «только в игре».

**Внешний вид** — жидкое стекло (панели блюрят обои под собой), GIF-обои
с блюром и затемнением, свои шрифты и цвета текста живьём, свои wav
на каждый звук интерфейса с громкостью, тема без перезапуска.

**Система** — локализация RU/EN переключается на лету, CDN-фикс для РФ
(hosts, нужен админ), автообновление лаунчера с GitHub, портативность:
всё в одной папке, реестр не трогается.

**Чат лаунчера** — в разработке, нестабилен: общий чат роблокс-сервера
и ЛС между игроками NekoStrap, нужен свой сервер (подробности ниже).

## Чат лаунчера (в разработке)

Чату нужен отдельный сервер, который поднимаешь сам — без него лаунчер
просто не сможет подключиться. Возможности могут меняться.

- Комната чата = PlaceId + JobId текущей сессии: кто на одном сервере —
  в одном чате, кто на разных — не пересекаются вообще.
- Общий канал сервера пишут все в комнате; ЛС доставляются только внутри
  одной комнаты и только между теми, кто запускает NekoStrap.
- Чат поднимается при входе в сессию и рвётся при выходе: вне игры
  общаться нельзя.
- Настройки → Чат лаунчера: вкл/выкл, ЛС, оверлей при входе в игру,
  адрес сервера, ник. То же самое есть в трее.
- Летающий оверлей поверх игры: перетаскивается зажатием шапки,
  сворачивается в полоску, «×» прячет без разрыва соединения. Работает
  в оконном и безрамочном режиме.

### Серверная часть

Код сервера лежит в `ChatServer/` и добавлен в `.gitignore` — в репозиторий
не публикуется. Отдельное консольное приложение на .NET 8 (Kestrel +
WebSocket), без NuGet-зависимостей.

```bash
dotnet run --project ChatServer          # слушает http://0.0.0.0:8787
# проверка: http://localhost:8787/health  →  {"ok":true,...}
```

Адрес в лаунчере: `ws://localhost:8787/ws` (Настройки → Чат).

### Развёртывание на VPS

Нужен крошечный VPS под круглосуточную работу (1 vCPU / 1 ГБ RAM хватает).
Подробно — в `ChatServer/README.md`, кратко:

| Провайдер | Регион | Цена |
|---|---|---|
| [Timeweb](https://timeweb.ru), [Beget](https://beget.com), [Selectel](https://selectel.ru) | РФ, оплата картой/СБП | от ~70–200 ₽/мес |
| [Hetzner](https://hetzner.com) | Европа | от ~€3.5/мес |
| [DigitalOcean](https://digitalocean.com) | США/Европа | от $4/мес |

ОС — Ubuntu 22.04/24.04 LTS, регион ближе к игрокам.

```bash
dotnet publish ChatServer -c Release -r linux-x64 --self-contained true -o dist
scp -r dist/* root@ТВ_IP:/opt/neko-chat/
```

```ini
# /etc/systemd/system/neko-chat.service
[Unit]
Description=NekoStrap ChatServer
After=network.target

[Service]
WorkingDirectory=/opt/neko-chat
ExecStart=/usr/bin/dotnet /opt/neko-chat/NekoStrapChatServer.dll
Restart=always
Environment=NEKO_CHAT_URLS=http://0.0.0.0:8787

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload && sudo systemctl enable --now neko-chat
```

Порт 8787 открой в панели провайдера и в `ufw` (`sudo ufw allow 8787/tcp`),
проверка — `http://IP:8787/health`. Для шифрования поставь Caddy перед
сервером (`reverse_proxy localhost:8787`) и получи `wss://` по домену.
Чтобы адрес стоял у всех игроков, впиши его в дефолт (`ChatServerUrl`
в `LauncherConfig`) перед сборкой exe.

Протокол (JSON over WebSocket), лимиты (500 символов, 10 сообщений/10 сек,
история 100 сообщений) — в `ChatServer/README.md`.

## Сборка из исходников

Нужны .NET 8 SDK и Windows.

```bash
dotnet restore
dotnet build NekoStrap.sln -c Release
```

Одиночный exe:

```bash
dotnet publish NekoStrap.Wpf/NekoStrap.Wpf.csproj -c Release -r win-x64 `
  --self-contained false /p:PublishSingleFile=true `
  /p:IncludeNativeLibrariesForSelfExtract=true -o build-wpf
```

Дымовой тест всего UI и логики (окна не показываются, код возврата —
число провалов):

```bash
dotnet run --project NekoStrap.Smoke -c Release
```

Сейчас в наборе 40 проверок: установка, моды и их откат, FastFlags,
профили, zip-импорт/экспорт, поиск, локализация, настройки.

## Релизы

Версия лежит в `<Version>` (`NekoStrap.Wpf.csproj`). Новый релиз —
тег и пуш:

```bash
git tag v1.3.0 && git push origin v1.3.0
```

Workflow на Windows собирает self-contained exe с версией из тега,
переименовывает в `NekoStrap-vX.Y.Z-win-x64.exe` и создаёт релиз.
Лаунчеры подхватывают его сами: соглашение для апдейтера — тег `vX.Y.Z`,
в ассетах один `.exe` с `win-x64` в имени.

## Структура

```
NekoStrap/
├── NekoStrap.sln
├── NekoStrap.Wpf/        # приложение: окно, страницы, тема, трей, оверлей чата
│   ├── Theme/Theme.xaml  # палитра, стили, иконки
│   ├── Pages/            # 7 разделов + диалоги
│   ├── Lang.cs           # движок локализации (RU/EN)
│   ├── LangStrings.cs    # словари строк
│   ├── Glass.cs          # жидкое стекло (блюр обоев под панелями)
│   ├── GifPlayer.cs      # анимированные GIF-обои
│   ├── ChatOverlayWindow.* # летающий оверлей чата (в разработке)
│   └── MainWindow.*      # хром окна + весь бэкенд
├── NekoStrap.Core/       # общая логика без UI (библиотека):
│   ├── Roblox/           #   установщик, запуск, версии, моды, флаги,
│   │                     #   сессии, Discord, целостность, поиск, чат (WebSocket)
│   ├── Media/            #   музыка (NAudio)
│   ├── Utils/            #   звуки интерфейса
│   ├── Display/          #   цветокоррекция (Magnification API)
│   └── Assets/           #   встроенные wav
├── ChatServer/           # сервер чата — в .gitignore, в репо не публикуется
├── NekoStrap.Smoke/      # дымовой тест UI
└── .github/workflows/    # CI: сборка релизного exe по тегу
```

## Данные

Всё лежит в `%LocalAppData%\NekoStrap` (или в своей папке, если задан
свой путь Roblox): `config.json`, версии клиента, `ModProfiles/` (профили
модов), `ModState/` (бэкапы для отката), `Music/`, флаги, история.
Реестр лаунчер не трогает.

## Лицензия

MIT — см. [LICENSE](LICENSE). Установка, FastFlags, Discord, Fleasion
и плеер — по мотивам Voidstrap (MIT).
