<Window x:Class="NekoStrap.Wpf.Pages.AddAccountDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить аккаунт" Width="480" Height="360" MinWidth="480" MinHeight="360"
        WindowStyle="None" ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgBrush}">
  <Window.Resources>
    <Style x:Key="DialogPassword" TargetType="PasswordBox">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="{StaticResource FgBrush}"/>
      <Setter Property="CaretBrush" Value="{StaticResource FgBrush}"/>
      <Setter Property="FontFamily" Value="{StaticResource MonoFont}"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Padding" Value="12,8"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="PasswordBox">
            <Border Background="{TemplateBinding Background}" CornerRadius="2"
                    BorderBrush="{StaticResource LineBrush}" BorderThickness="1"
                    Padding="{TemplateBinding Padding}">
              <ScrollViewer x:Name="PART_ContentHost"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>
  <Border BorderBrush="{StaticResource LineBrush}" BorderThickness="1" Padding="20,16">
    <StackPanel>
      <TextBlock Style="{StaticResource H2}" Text="Добавить аккаунт" Margin="0,0,0,10"/>
      <TextBlock Style="{StaticResource Body}" FontSize="12"
                 Text="Кука .ROBLOSECURITY (roblox.com → DevTools → Application → Cookies)" Margin="0,0,0,6"/>
      <PasswordBox x:Name="CookieBox" Style="{StaticResource DialogPassword}" Margin="0,0,0,2"/>
      <TextBox x:Name="CookieOpenBox" Style="{StaticResource DarkInput}" Visibility="Collapsed" Margin="0,0,0,2"/>
      <CheckBox x:Name="ShowCookie" Style="{StaticResource Switch}" Margin="0,4,0,0"
                Checked="ShowCookie_Changed" Unchecked="ShowCookie_Changed">
        <TextBlock FontFamily="{StaticResource UiFont}" FontSize="12" Foreground="{StaticResource FgBrush}" Text="Показать куку"/>
      </CheckBox>
      <TextBlock Style="{StaticResource Body}" FontSize="12"
                 Text="Подпись (необязательно): например «Основа», «Твинк»" Margin="0,8,0,6"/>
      <TextBox x:Name="AliasBox" Style="{StaticResource DarkInput}" MaxLength="40" Margin="0,0,0,10"/>
      <StackPanel Orientation="Horizontal">
        <Button Style="{StaticResource PrimaryButton}" Content="Добавить и проверить" Width="200" Height="38" Click="OkButton_Click"/>
        <Button Style="{StaticResource OutlineButton}" Content="Отмена" Width="120" Height="38" Margin="10,0,0,0" Click="CancelButton_Click"/>
      </StackPanel>
      <TextBlock Style="{StaticResource Mono}" FontStyle="Italic" TextWrapping="Wrap" Margin="0,10,0,0"
                 Text="Кука никуда не отправляется кроме api Roblox и хранится шифром на этом ПК."/>
    </StackPanel>
  </Border>
</Window>
